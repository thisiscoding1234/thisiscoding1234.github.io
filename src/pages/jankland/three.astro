---
import Jankbar from '../../components/Jankbar.astro';
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wonderfully Janky</title>
    <style>
        body {
            font-family: 'Comic Sans MS', cursive;
            background-color: #F0E68C;
            color: #222;
            overflow-x: hidden;
            animation: backgroundFlash 4s infinite alternate;
        }

        h1 {
            font-size: 3em;
            text-align: center;
            transform: rotate(-2deg);
            animation: textWobble 1s infinite;
        }

        p {
            font-size: 1.2em;
            line-height: 1.5;
            width: 90%;
            margin: 1em auto;
            text-align: justify;
        }

        .container {
            background-color: #FF6347;
            width: 80%;
            margin: auto;
            padding: 20px;
            border: 5px dashed #222;
            border-radius: 20px;
            text-align: center;
            animation: borderPulse 2s infinite;
        }

        .btn {
            font-size: 1.2em;
            color: #FFF;
            background-color: #222;
            padding: 10px 20px;
            border: none;
            cursor: pointer;
            transform: rotate(-3deg);
            transition: 0.2s;
        }

        .btn:hover {
            background-color: #FF4500;
            transform: rotate(3deg);
        }

        @keyframes backgroundFlash {
            from { background-color: #F0E68C; }
            to { background-color: #FFD700; }
        }

        @keyframes textWobble {
            0%, 100% { transform: rotate(-2deg); }
            50% { transform: rotate(2deg); }
        }

        @keyframes borderPulse {
            0%, 100% { border-color: #222; }
            50% { border-color: #0000FF; }
        }
    </style>
</head>
<body>
    <Jankbar/>
    <h1>Wonderfully Janky</h1>
    <div class="container">
        <p>Welcome to a site that's intentionally a bit... off. We embrace imperfections and quirks here, so feel free to explore the wonderful world of jank!</p>
        <button class="btn">Click Me!</button>
    </div>

</body>
</html>
